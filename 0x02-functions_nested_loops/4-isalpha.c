#include "main.h"
/**
 * _isalpha - Test the _isalpha function
 * @n: Number to pass to _isalpha function
 * 
 * Return: 1 (Success)
 */
int _isalpha(int c)
{
if (case 'A' ... 'Z' && 'a' ... 'z')
return (1);
else
return (0);
}

#include "main.h"
#include <stdio.h>
/**
 * @n: integer
 * @i: integer
 * @a: integer
 * @b: integer
 * @sum: integer
 * Return: Always 0.
 */
int main(void)
{
int main(void)
{
int i,n,a,b,sum;
while (i < 1024)
{
n = 3 * i;
i++;
printf("%d", n);
}
while (a < 1024)
{
b = 5 * a;
a++;
printf("%d", b);
}
printf("%d + %i", n, b);
printf("\n");
return (0);
}

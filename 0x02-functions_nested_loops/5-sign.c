#include "main.h"
/**
 * int print_sign - checks for alphabetic character
 * @n: Number to pass to _isalpha function
 *
 * Return: 1 (Success)
 */
void int print_sign(int n)
{
	if (n > 0)
		return (+);
	else if (n = 0)
		return (0);
	else 
		return (-);
}

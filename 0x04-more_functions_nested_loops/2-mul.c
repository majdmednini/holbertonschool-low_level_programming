#include "main.h"
/**
 * int mul - multiplies two integers
 * @a: The character to check
 * @b: The character to check
 * Return: Always 0 
 */
int mul(int a, int b);
{
{
printf(a * b);
}
return (0);
}

#include "main.h"
#include <stdio.h>
/**
 *  *print_line - draws a straight line
 *   * Return: Always 0.
 *    */
void print_line(int n)
{
	int main()
	{
		char str[100];
		int i, totChar, n;
		totChar = 0;
		for (i=0; str[i] != '\0'; i++)
		{
			if (str[i]!=' ')
			{
				totChar++;
			}
			if (i != 0)
			{
				for ( n = 0; n < i; n++)
				{
					printf("_")
				}
			}
		}
	}
}

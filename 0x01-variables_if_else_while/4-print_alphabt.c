#include <stdlib.h>
#include <time.h>
#include <stdio.h>
#include <math.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char ch,chh,c;
for (ch = 'a' ; ch <= 'd' ; ch++)
{
putchar(ch);
}
for (c = 'f' ; c <= 'p' ; c++)
{
putchar(ch);
}
for (chh = 'r' ; chh <= 'z' ; chh++)
{
putchar(ch);
}
putchar('\n');
return (0);
}

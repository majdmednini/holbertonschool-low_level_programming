#include "main.h"
#include <stdio.h>
/**
 * swap_int - check the code
 *@a: The character to check
 *@b: The character to check
 * Return: Always 0.
 */
void swap_int(int *a, int *b)
{
{
*a = %d, a
*b = %d, b
}
{
swap_int(&*a, &*b);
}
{
printf("%d, %d\n", *a, *b);
}
}

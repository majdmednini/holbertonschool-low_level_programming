#include "main.h"
/**
 * _strncpy - prints every other character of a string
 *@dest: char
 *@src: char
 *@n: integer
 * Return: 0
 */
char *_strncpy(char *dest, char *src, int n)
{
strncpy(dest, src, 98);
for (int i = 0; i < ; i++)
{
printf("%c", dest[i]);
}
return (0);
}

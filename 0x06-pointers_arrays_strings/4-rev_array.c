#include "main.h"
/**
 * reverse_array - compares two strings
 *@a: int
 *@n: int
 * Return: 0
 */
void reverse_array(int *a, int n)
{
int i;
i = 0;
while (i < n && src[i] != '\0')
{
dest[i] = src[i];
i++;
}
i = i;
while (i < n)
{
dest[i] = '\0';
i++;
}
return (0);
}
